# 조직효과성 진단 참고자료

이 폴더는 조직효과성 진단 기능 개발을 위한 참고자료를 포함합니다.

## 📁 파일 설명

### 1. **index.xlsx** (19KB)
조직효과성 진단의 문항 구조 및 분류 정보

**내용:**
- 총 52개 문항 (헤더명, 문항명, 소분류, 대분류)
- 개인정보 필드 (8개)
- Input 영역 문항 (20개)
  - 조직 관점 (7개): 목표와 방향, VWBE 실천환경
  - 구성원 관점 (9개): 역할/책임, 역량, 태도/가치
  - 환경 관점 (4개): R&C 확보, 제도/System
- Process 영역 문항 (13개)
  - SKMS 실천: 도전적 목표, 과감한 실행, 적극적 소통, Teamwork, 외부 협업
- Output 영역 문항 (9개)
  - 조직 성과 (4개): 목표달성, 적시성, 혁신성, 지속가능성
  - 구성원 행복 (5개): 긍정정서, 일의 가치, 성취감, 개인성장, 미래기대
- 주관식 (4개): 조직 이미지, 강점, 보완점, 장애요인

**컬럼:**
- 헤더명: Excel 파일의 실제 컬럼명 (CMPNAME, POS, NO1~NO43 등)
- 문항명: 사용자 친화적인 문항 설명
- 소분류: 세부 카테고리
- 대분류: IPO 모델의 주요 영역

**용도:**
- `modules/data_processor.py`의 컬럼 매핑 정의 시 참조
- 데이터 검증 로직 구현 시 사용
- AI 분석 프롬프트 작성 시 문항 정보 활용

---

### 2. **rawsample.xlsx** (18KB)
실제 진단 응답 샘플 데이터

**내용:**
- 응답자 수: 23명 (SK하이닉스 직원)
- 정량 데이터: NO1~NO39 (5점 척도 응답)
- 정성 데이터: NO40~NO43 (텍스트 응답)

**주요 특징:**
- 실제 응답 분포 확인 가능
- 5점 척도: 1(매우 낮음) ~ 5(매우 높음)
- 주관식 응답 길이: 10~200자
- 일부 결측치 존재 (NO44, NO45는 대부분 NaN)

**활용:**
- 테스트 데이터로 사용 (`tests/test_organizational_effectiveness.py`)
- 데이터 전처리 로직 검증
- AI 분석 결과 품질 확인
- PDF 리포트 레이아웃 테스트

**샘플 통계:**
- NO1 (명확한 목표): 평균 4.57 (표준편차 0.66)
- NO2 (우선순위 설정): 평균 4.35 (표준편차 0.78)
- NO35 (긍정적 정서): 평균 4.39 (표준편차 0.66)

---

### 3. **format.xlsx** (906KB)
현재 제공 중인 개인별 리포트 포맷

**내용:**
- 시트: Personal.Report (1,156행)
- 기존 리포트 양식 및 레이아웃
- 수식, 차트, 서식 정보 포함

**특징:**
- SK하이닉스 스타일의 리포트 디자인
- 복잡한 레이아웃 구조
- 개인별 리포트 형식 (우리는 조직 전체 리포트로 변경 필요)

**활용:**
- 기존 디자인 컨셉 참고
- 색상 팔레트, 폰트 스타일 추출
- 섹션 구조 아이디어
- **주의**: 직접 사용하지 않고 참고용으로만 활용

**차이점:**
| 항목 | 기존 (format.xlsx) | 신규 (우리가 만들 것) |
|------|-------------------|---------------------|
| 대상 | 개인 (360도 평가) | 조직/팀 전체 |
| 포맷 | Excel | PDF (HTML/CSS 기반) |
| 데이터 | 관계별 비교 | IPO 영역별 분석 |
| 분량 | 1,156행 | 20-30페이지 PDF |

---

## 🎯 개발 시 활용 방법

### Phase 1: 데이터 모델링
```python
# index.xlsx를 읽어서 컬럼 매핑 자동 생성
import pandas as pd

index_df = pd.read_excel('docs/reference/organizational-effectiveness/index.xlsx')

# NO1~NO43을 IPO 영역별로 그룹화
input_questions = index_df[index_df['대분류'] == '조직 관점 Input ']
process_questions = index_df[index_df['대분류'] == 'SKMS 실천 Process ']
output_questions = index_df[index_df['대분류'] == 'Output ']
```

### Phase 2: 테스트 데이터 로드
```python
# rawsample.xlsx로 테스트
sample_df = pd.read_excel('docs/reference/organizational-effectiveness/rawsample.xlsx')

# 기본 통계 확인
print(sample_df[['NO1', 'NO2', 'NO3']].describe())

# 결측치 확인
print(sample_df.isnull().sum())
```

### Phase 3: 디자인 참고
```python
# format.xlsx에서 색상 팔레트 추출
import openpyxl

wb = openpyxl.load_workbook('docs/reference/organizational-effectiveness/format.xlsx')
# 기존 색상, 폰트, 레이아웃 분석...
```

---

## 📊 IPO 모델 구조

```
INPUT (20문항)
├── 조직 관점 (7문항)
│   ├── 목표와 방향 (NO1-3)
│   └── VWBE 실천환경 (NO4-7)
├── 구성원 관점 (9문항)
│   ├── 역할과 책임 (NO8-9)
│   ├── 구성원 역량 (NO10-11)
│   └── 태도/가치 (NO12-14)
└── 환경 관점 (4문항)
    ├── R&C 확보 (NO15-16)
    └── 제도/System (NO17-18)

PROCESS (13문항)
├── 도전적 목표 (NO19-21)
├── 과감한 실행 (NO22-23)
├── 적극적 소통 (NO24-25)
├── Teamwork 발휘 (NO26-28)
└── 외부 협업 (NO29-30)

OUTPUT (9문항)
├── 조직 성과 (NO31-34)
│   ├── 목표 달성
│   ├── 적시성
│   ├── 혁신성
│   └── 지속가능성
└── 구성원 행복 (NO35-39)
    ├── 긍정적 정서
    ├── 일의 가치
    ├── 성취감
    ├── 개인 성장
    └── 미래 기대
```

---

## ⚠️ 주의사항

1. **개인정보 보호**
   - rawsample.xlsx에는 실명, 이메일 등 개인정보 포함
   - Git에 커밋하지 않도록 `.gitignore`에 추가 필요
   - 테스트 시 마스킹 처리

2. **저작권**
   - format.xlsx는 SK하이닉스의 기존 포맷
   - 디자인 참고만 하고 직접 복사하지 않기
   - 독자적인 디자인 개발

3. **데이터 품질**
   - NO44, NO45는 거의 비어있음 (사용 안 함)
   - 일부 응답에 결측치 존재 → 처리 로직 필요
   - 주관식 응답에 특수문자 포함 → 이스케이프 처리

---

## 📝 다음 단계

1. ✅ 참고자료 복사 완료
2. ⬜ Spec 작성 (`specs/002-organizational-effectiveness-diagnosis/spec.md`)
3. ⬜ 데이터 모델 정의 (`data-model.md`)
4. ⬜ 구현 계획 수립 (`plan.md`)
5. ⬜ Task 분해 (`tasks.md`)

---

*최종 업데이트: 2025-10-27*
